# Ralph Progress Log
Started: Fri Apr 17 14:53:15 CST 2026
---

## Codebase Patterns
- Backend uses `uv` for dependency management; install with `uv sync --extra dev`.
- Backend quality gates: `uv run ruff check .`, `uv run mypy`, `uv run pytest` (run from `backend/`).
- mypy is configured with `strict = true` and `files = ["app", "tests"]`; every new function needs type annotations.
- FastAPI app lives at `backend/app/main.py` and is served via `uvicorn app.main:app --reload`.
- CORS is pinned to `http://localhost:5173` for the Vite frontend.
- Tests use `fastapi.testclient.TestClient`; place them in `backend/tests/` with `__init__.py`.
- HTTP endpoints live under `backend/app/routers/<name>.py` and are attached to `app` in `main.py` via `app.include_router(...)`. Use `APIRouter(prefix="/api", tags=[...])` for all feature endpoints.
- Use `Annotated[UploadFile, File(...)]` style for FastAPI dependencies — avoids ruff B008 while staying mypy-clean.
- Filesystem paths for derived assets live in `backend/app/storage.py` (`IMAGES_DIR`, etc.). Import the module (not the constant) so tests can `monkeypatch.setattr(storage, "IMAGES_DIR", tmp_path)`.
- `backend/storage/` is gitignored; create subdirs with `mkdir(parents=True, exist_ok=True)` on first write.
- Anthropic SDK calls need `cast(ToolParam, ...)` / `cast(ToolChoiceToolParam, ...)` / `list[MessageParam]` typing because the Messages.create overloads reject free-form dicts under strict mypy.
- For Anthropic-backed endpoints, expose a module-level `get_anthropic_client()` factory so tests can `monkeypatch.setattr(router_module, "get_anthropic_client", lambda: stub)`; keep the real function returning `anthropic.Anthropic()` (reads `ANTHROPIC_API_KEY`).

---

## 2026-04-17 - US-001
- Implemented FastAPI skeleton with `/health` endpoint returning `{"status": "ok"}`.
- Added CORSMiddleware allowing `http://localhost:5173`.
- Added pytest coverage for health response and CORS preflight.
- Wrote `backend/README.md` with setup/run/test instructions and env var list.
- Verified `uv run uvicorn app.main:app` starts and serves `/health` 200.
- Files changed:
  - backend/app/main.py (new)
  - backend/tests/__init__.py (new)
  - backend/tests/test_health.py (new)
  - backend/README.md (new)
- **Learnings for future iterations:**
  - `uv sync --extra dev` picks up `[project.optional-dependencies].dev` and installs pytest/ruff/mypy/httpx together.
  - TestClient in FastAPI 0.136 / Starlette 1.0 returns 200 (not 204) on OPTIONS preflight when CORS is configured — assert 200.
  - `pyproject.toml` already enforces `pythonpath = ["."]` and `testpaths = ["tests"]`, so imports use `from app.main import app`.
  - mypy strict mode requires explicit return types even on trivial handlers (e.g. `-> dict[str, str]`).
---

## 2026-04-17 - US-002
- Added `POST /api/upload` accepting multipart `file`; validates ≤10MB and PNG/JPEG/WebP via Pillow; stores to `backend/storage/images/{uuid}.{ext}`; returns `{image_id, width, height}` with 201.
- Introduced `backend/app/routers/` package and `backend/app/storage.py` module for filesystem paths.
- Added Pillow 12 as a runtime dependency.
- pytest covers PNG/JPEG/WebP success, oversized (with patched `MAX_FILE_SIZE_BYTES`), invalid bytes, and unsupported GIF.
- Updated `backend/README.md` with a curl example.
- Files changed:
  - backend/pyproject.toml, backend/uv.lock
  - backend/app/main.py (include router)
  - backend/app/routers/__init__.py (new)
  - backend/app/routers/upload.py (new)
  - backend/app/storage.py (new)
  - backend/tests/test_upload.py (new)
  - backend/README.md
- **Learnings for future iterations:**
  - Prefer `Annotated[UploadFile, File(...)]` over bare `File(...)` defaults — keeps ruff's B008 quiet while mypy stays happy.
  - Pillow 12 ships `py.typed`; no stub package needed for strict mypy.
  - Test images should be generated on-the-fly with `PIL.Image.new(...).save(buf, format=...)` — avoids fixture files and keeps the repo clean.
  - To test oversized rejection without big payloads, `monkeypatch.setattr(upload, "MAX_FILE_SIZE_BYTES", small)` and send a modestly larger payload.
  - Import the `storage` module (not the constant directly) so tests can monkeypatch `storage.IMAGES_DIR` per-test via `tmp_path`.
---

## 2026-04-17 - US-003
- Added `POST /api/perception` that base64-encodes the stored image, calls Claude (`claude-opus-4-7`) with a forced `report_elements` tool_use, and caches the structured `{id, label, bbox, z_order}` list to `backend/storage/perception/{image_id}.json`. Returns cached response on repeat calls.
- Exposed `get_anthropic_client()` factory so tests swap in a stub; missing image returns 404; malformed VLM response returns 502.
- Added `anthropic` runtime dependency and `PERCEPTION_DIR` in `backend/app/storage.py`.
- Tests cover success + cache-write, cache hit skipping the VLM, missing image, and unstructured response.
- Files changed:
  - backend/pyproject.toml, backend/uv.lock
  - backend/app/main.py (include perception router)
  - backend/app/routers/perception.py (new)
  - backend/app/storage.py (add PERCEPTION_DIR)
  - backend/tests/test_perception.py (new)
  - backend/README.md (document endpoint + env var)
- **Learnings for future iterations:**
  - Anthropic Python SDK 0.96: `messages.create` overload rejects loose dicts; cast `tools`/`tool_choice`/`messages` to their SDK types (`ToolParam`, `ToolChoiceToolParam`, `list[MessageParam]`) to keep strict mypy happy without giving up the readable dict form.
  - `media_type` on the image source is a `Literal` union in the SDK; `cast(Any, media_type)` is the lightest way to pass a dynamic extension-derived string.
  - Use `block.input` on tool_use blocks — it's already a `dict`, no `json.loads` needed. Guard with `isinstance(block.input, dict)` to satisfy mypy and reject weird results.
  - Stub the Anthropic client with a plain class exposing `.messages.create(**kwargs)`; no need to import `anthropic.types.Message` for tests.
  - `fastapi.testclient.TestClient` sends JSON bodies with `client.post(url, json=...)`; perception endpoint is JSON-only (no multipart).
---
